trigger:
- main

resources:
- repo: self

variables:
  tag: '$(Build.BuildId)'

stages:
- stage: Build
  displayName: Build image
  jobs:
  - job: Build
    displayName: Build
    pool:
      name: agents
    steps:
    - script: |
        /home/azdouser/tools/mgc_cli/mgc -v
      displayName: Verify MGC CLI version